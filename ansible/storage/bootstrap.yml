---

- name: Storage
  hosts: storage
  user: david
  become: yes

  roles:
    - { role: ../roles/base, tags: base }
    - { role: storage, tags: storage }
    - { role: samba, tags: samba }
    - { role: iptables, tags: iptables }
