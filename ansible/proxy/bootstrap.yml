---

- name: Setup proxy
  hosts: proxy
  user: david
  become: yes

  vars:
    certbot_domains:
      - domain: "network.home.dmarby.se"
        delegated: "home.dmarby.se"

  roles:
    - ../roles/expand_root
    - ../roles/base
    - ../roles/certbot
    - ../roles/nginx
    - proxy
