---

- name: Setup dofler
  hosts: dofler
  become: yes

  vars:
    wan_interface: ens160

  roles:
    - ../roles/expand_root
    - ../roles/base
    - ../roles/docker/daemon
    - ../roles/docker/expose
    - network
